pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.dart"
      run: dart format --set-exit-if-changed {staged_files}
    analyze:
      run: flutter analyze --fatal-infos

pre-push:
  commands:
    test:
      run: flutter test
